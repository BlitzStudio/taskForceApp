import React from "react";
import { Text, View } from "react-native";

export default function Targets() {
    return (
            <View > 
                <Text >Targets</Text>
            </View>
        );
}